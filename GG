def addition(first_number, second_number):
  result = first_number + second_number
  return result

# calling the function passing as arguments the values 1 and 2, the returned result is stored as a
a = addition(1, 2) 
print(a) # prints the value of variable a: 3
